/**
 * Canonical Navigation Motion Re-export
 * Single source of truth defined in @workspace/livex-core
 */
export {
  type NavSpringConfig,
  type NavReducedMotionConfig,
  CANONICAL_NAV_GEOMETRY,
  CANONICAL_NAV_MOTION,
  resolveDragDestination,
} from '@workspace/livex-core';
